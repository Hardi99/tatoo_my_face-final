{{ form_start(form) }}
<div class="row">
    <div class="col-md-4">{{ form_row(form.firstname) }}</div>
    <div class="col-md-4">{{ form_row(form.lastname) }}</div>
    <div class="col-md-4">{{ form_row(form.email) }}</div>
</div>
<div class="row">
    <div class="col-md-4">{{ form_row(form.appointmentAt) }}</div>
    <div class="col-md-4">{{ form_row(form.start) }}</div>
    <div class="col-md-4">{{ form_row(form.end) }}</div>
</div>
<button class="btn btn-primary">{{ button|default('Enregistrer') }}</button>
{{ form_end(form) }}